# --- Sensitive production data (PII) — NEVER commit/push, keep local only ---
Reference Files/
05. Hotel Ops/
APPA_Notes_Compiled.md
*.xlsx
*.pdf
# real Travel Memos / passports / itineraries if ever placed elsewhere
*passport*
*Passport*

# --- Google service-account secrets — NEVER commit ---
*service_account*.json
*service-account*.json
*.credentials.json

# --- Build artifacts ---
*.app/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
