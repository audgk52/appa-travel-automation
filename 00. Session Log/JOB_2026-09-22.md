# Daily Retrospective — 2026-09-22

## 1. 오늘의 목표

- Hotel Ops가 Dispatch의 Google Sheet를 잘못 참조하지 않도록 live target을 분리한다.
- 실제 PII-free throwaway Rooming List에서 LIVE-1을 시작한다.
- A1 `rooming_record_id` adoption을 안전하게 실행한다.
- B1 정상 업무 변경(Echo checkout 연장)의 live 실행 전 계약과 운영 경계를 검증한다.

## 2. 완료한 작업

### Hotel Ops target isolation

- Hotel Ops 전용 `APPA_HOTEL_GSHEET_ID` 경계를 추가했다.
- 기존 Dispatch `APPA_GSHEET_ID`로의 fallback을 금지했다.
- Hotel target이 Dispatch target과 같으면 fail closed하도록 했다.
- 물리적 header가 row 1이 아니라 row 4인 실제 Sheet 구조를 지원하는 preflight를 검증했다.
- PII-free throwaway Sheet의 title, tab, gid, header, Payment 값, Request History, system columns를 확인했다.

### LIVE-1 A1 실행

- read-only preflight와 exact preview를 거쳐 PO가 승인한 동일한 frozen plan만 실행했다.
- 한 번의 `spreadsheets.batchUpdate`로 정확히 세 셀만 수정했다.

| Cell | Record | Adopted `rooming_record_id` |
|---|---|---|
| R8 | Test Traveler Charlie | `rl-cvpq1sc73nfqmb7atdi7b290` |
| R9 | TBD - DP | `rl-kkuh50gop63dq9pcf9kp88vc` |
| R13 | Test Traveler Golf | `rl-nqs9q94fm9uuq7gtd216kpi4` |

- `stay_id`, business fields, Request History, yellow formatting, drafts에는 쓰지 않았다.
- 결과는 `VERIFIED`였고 unauthorized write는 0건이었다.

### A1 safety remediation 및 감사

- actual backend destination binding
- frozen header/id-column/write-range 검증
- exact three-cell atomic request
- API exception 후 read-back reconciliation
- duplicate ID, changed target, removed/moved/ineligible target 검증
- unresolved title review가 있을 때 `all PASS`를 표시하지 않는 preflight semantics

A1 최종 감사는 commit `aa98a623946378f07186af7e7dd57297cdd9aaa3`에서 CLEAN을 받았다.

### B1 후보 및 exact preview

- B1 후보로 `Test Traveler Echo`를 선택했다.
- 제안한 변경:
  - Check-out: `11/10/2026 → 11/12/2026`
  - Total # of Nights: raw OLD `'  11 '` → `13`
  - Request History append
  - Kakao/email draft 생성, 실제 전송 없음
- `stay-echo`는 established single-member stay이고 관련 dependent record가 없음을 확인했다.
- early-check-in 정책이나 payer 판단이 필요하지 않은 checkout-only change임을 확인했다.

### Request History 표현 정리

- raw Sheet evidence와 revalidation 값은 그대로 보존했다.
- 사람이 읽는 Request History만 whitespace를 정규화했다.
- 승인된 B1 표현:

```text
* 0922 check-out 11/10/2026 → 11/12/2026, nights 11 → 13
```

### Durable StateStore 및 operational boundary 보강

- 임시 scratch StateStore가 restart/idempotency 계약을 충족하지 못한다는 점을 발견했다.
- Hotel Ops 전용 JSON StateStore 설정과 target binding을 도입했다.
- supported live facade를 추가해 Sheet/StateStore를 caller가 임의 주입하지 못하도록 보강했다.
- non-empty unbound state, canonical state path, destination identity failure, post-write exception을 fail closed하도록 반복 보완했다.
- preview → human confirmation → execution을 연결하는 value-owned artifact 계약을 추가했다.

관련 커밋 흐름:

- `71f68c765ce276e8e55d664a1acd4bac438cbe4c` — Request History rendering
- `3ead589804a4b6415a07a9ed5692288368fac175` — authoritative StateStore
- `9887afb239321d1bf481a82fc751e6e44f9fa729` — live operational facade 및 failure handling
- `9fc8a03445905d92789e2f19598255df0da89688` — preview/confirmed artifact 및 draft recovery contract

현재 full Hotel Ops regression은 461 passed이며, `TMPDIR=/private/tmp`에서도 461 passed이다.

## 3. 오늘 잘된 점

- 잘못 설정된 Sheet가 Dispatch production target임을 확인한 순간 쓰기를 중단했다. Fail-closed 원칙이 실제 데이터 손상을 막았다.
- A1에서 row number나 NAME을 identity로 오인하지 않고, exact preview와 backend destination을 결합해 세 셀만 수정했다.
- 단위 테스트 통과만으로 live readiness를 선언하지 않고 Codex adversarial audit을 반복했다.
- 기술 문제를 단순 버그로 처리하지 않고 data ownership, durable authority, human confirmation, recovery contract 문제로 분리했다.
- Request History에서 raw evidence와 human-readable rendering의 소유권을 분리했다.

## 4. 어려웠던 점 / 예상과 달랐던 점

- 안전한 helper를 추가하는 것과 실제 operational path가 그 helper만 사용하도록 강제하는 것은 다른 문제였다.
- 기존 테스트는 low-level injectable functions 중심이어서 실제 live call graph의 우회를 충분히 드러내지 못했다.
- preview, confirmation, execution, restart recovery 사이에 동일한 artifact identity를 유지하는 계약이 초기 Foundation에 완전히 연결되어 있지 않았다.
- post-write 성공 여부뿐 아니라 draft 같은 비파괴 output의 실패 상태와 recovery까지 정의해야 했다.
- remediation → audit 과정이 반복되며 B1 자체의 live 실행은 오늘 완료하지 못했다.

## 5. 근본 원인

- Foundation에서 business logic과 adapter 분리는 잘했지만, 실제 운영 entrypoint와 durable state authority를 하나의 end-to-end contract로 먼저 검증하지 않았다.
- in-memory/test dependency injection의 유연성이 live operational boundary에도 그대로 노출됐다.
- `RoomingChange`의 business identity는 있었지만, PO가 본 preview artifact와 durable recovery artifact를 동일하게 묶는 envelope가 늦게 추가됐다.
- 정상 성공 경로보다 partial/uncertain/restart 경로의 실제 호출 순서를 뒤늦게 검증했다.

## 6. 오늘 확정한 결정

- Hotel Ops live target은 Dispatch target과 완전히 분리한다.
- PG v1 durable operational state는 한 머신의 Hotel Ops 전용 JSON StateStore로 유지한다.
- preview, confirmation, execution, recovery는 supported `live_ops` facade를 통해야 한다.
- PO가 확인한 exact semantic artifact와 승인 결정이 execution/recovery까지 보존되어야 한다.
- Request History의 raw evidence는 보존하고 표시만 정규화한다.
- draft 실패 때문에 검증된 Sheet/history 효과를 다시 쓰지 않는다.
- 실제 Kakao/email 전송은 계속 out of scope다.
- B1은 artifact/recovery 계약이 CLEAN을 받기 전에는 live preview와 mutation을 진행하지 않는다.

## 7. 미완료 / 현재 blocker

최신 Codex 감사 기준으로 다음 artifact/recovery 항목이 남아 있다.

1. 동일 preview와 동일 승인 결정이 동일 ConfirmedArtifact/operation identity를 만들어야 한다.
2. policy decision은 key뿐 아니라 실제로 제시된 allowed value까지 검증해야 한다.
3. ConfirmedArtifact가 승인된 PreviewArtifact digest를 포함해야 한다.
4. durable recovery가 destination과 전체 ConfirmedArtifact/digest를 보존·검증해야 한다.
5. legacy recovery가 `MMDD`나 `hotel_confirmed=False`를 임의로 만들면 안 된다.
6. 반복 draft 실패의 top-level status가 `noop_already_done`이면 안 된다.
7. Request History read-back ambiguity는 `failed`가 아니라 `uncertain`이어야 한다.

현재 B1 business mutation은 실행하지 않았다.

## 8. 다음 세션 우선순위

1. Round 4 artifact identity/recovery remediation을 하나의 새 commit으로 완료한다.
2. 동일 preview instance + 동일 decision의 deterministic confirmation을 검증한다.
3. full ConfirmedArtifact를 Sheet mutation 전에 durable state에 저장한다.
4. legacy missing semantics를 명시적 incompatible 상태로 처리한다.
5. draft/history ambiguity 상태를 truthfully report하도록 수정한다.
6. narrow Codex re-audit에서 CLEAN을 받는다.
7. 그 후에만 새 B1 read-only preview → PO 승인 → B1 live execution을 진행한다.

## 9. 포트폴리오에 남길 근거

- production Sheet 오지정 가능성을 live write 전에 탐지하고 cross-agent target isolation으로 차단했다.
- 하나의 human approval이 exact Sheet updates, nights, history, drafts를 구동하도록 계약을 설계했다.
- AI 구현 결과를 그대로 수용하지 않고, 실제 operational call graph와 failure/recovery behavior를 adversarial audit했다.
- atomic three-cell A1 adoption을 실제 Google Sheet에서 검증했다.
- 461개 회귀 테스트와 별도의 live safety gate를 유지했다.
- 인간의 판단과 자동화 가능한 반복 작업의 경계를 제품 계약으로 명시했다.

## 10. 상태 요약

- A1 ID adoption: **LIVE VERIFIED**
- B1 normal business change: **BLOCKED — artifact/recovery closure pending**
- Hotel Ops Foundation: **구현 대부분 완료, live operational envelope 최종 보완 중**
- Google Sheet unauthorized writes: **0**
- Telegram: **미착수**
- Document Agent: **미착수**
- Current branch: `hotel-ops-target-isolation`
- Current reported HEAD: `9fc8a03445905d92789e2f19598255df0da89688`
- Reported working tree: clean
- Push status at end of session: not pushed
