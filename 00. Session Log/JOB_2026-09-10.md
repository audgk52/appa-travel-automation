# JOB — 2026-09-10 (APPA Agent Automation)

**Session:** Dispatch — Calendar retest, Phase C (Google Sheets source of truth) build + concurrency-safe revision, PR #2, and full live Sheets verification. 2026-09-10 KST.
**Working folder:** `~/Desktop/2026 취준/Travel Automation/`
**Branch:** `phase-c-sheets` (commit `e9cbd4a`) · **PR #2** open, NOT merged (held for owner review).

---

## Key Points (what happened)

1. **Session recap** — resumed after a gap; memory was stale (thought Phase B was current). Verified on disk that Phase A+B were shipped (73 tests, Calendar live) and updated memory.

2. **Calendar notification retest** — the 8/14 09:00 mock hadn't visibly fired. Built fresh mock TMOs **Test Traveler A/B** (dispatch 2026-09-12 → Send Date 2026-09-10) and ran the REAL CLI; created 2 Calendar events for **2026-09-10 09:00 KST**, `reminders.useDefault=true`, verified via API. Clarified the ownership model: `useDefault` resolves per-user from the human's own default reminder on the shared calendar — the true test is whether it fires on the user's device.
   - **Excel-clobber incident:** the CLI wrote A/B rows to `master_schedule.xlsx`, but the file was open in Excel, which re-saved its stale copy and deleted the rows (ctime revealed the second write). Restored by quitting Excel and re-running. Recorded as a feedback memory. This reinforced the motivation for the Sheets migration.

3. **Phase C — Google Sheets migration (BUILT, TDD).** `GoogleSheetStore` replaces Excel as the operational source of truth, same `upsert(row)->inserted|updated` contract. Sheets-first Calendar gating (Sheet success→Calendar; Sheet fail→skip Calendar, exit≠0). New `config.sheet_config()`/`SheetConfigError`, env `APPA_GSHEET_ID` (+ optional `APPA_GSHEET_TAB`), reuses `APPA_GOOGLE_SA_KEY`; calendar enable-signal changed to the calendar id alone (SA key now shared). Excel/`ScheduleStore` kept only as optional export.

4. **Senior-review concern → concurrency-safe revision.** Reworked `GoogleSheetStore` for a shared, human-editable sheet: server-side `values.append` for new rows, in-place `values.update` on only `A{r}:N{r}`, server-side `batchUpdate sortRange` by Send Date (whole rows move, human cols O+ preserved) — never a whole-tab rewrite. Added a **system-owned `Record ID`** column (N) = `rid`+base32hex(normalized Name+Direction); legacy rows without one are matched by (Name,Direction) and back-filled, no dupes. Documented residual locate→write race (optimistic locking out of scope).

5. **Committed + PR.** `e9cbd4a` on `phase-c-sheets`, **PR #2** against `main`. **96 tests passing** throughout. Docs: `SETUP_GoogleSheets.md`, `DESIGN_Dispatch_PhaseC_Sheets.md`; corrected stale local-Excel wording in `SETUP_GoogleCalendar.md`.

6. **LIVE Google Sheets verification (real APIs).** Enabled Sheets API in project `appa-dispatch`; primary sheet `10Eidxjb…OJP7g`, throwaway copy `1EI45RX…P2MOKk`.
   - **Section B (core): ALL PASS** — insert, idempotent re-run, revision, server-side sort, human side-column preserved through sort, Sheets→Calendar gating (bad Sheet write → `[ERROR]`, no event; success → event w/ KakaoTalk msg in body). `master_schedule.xlsx` provably not written (sha1 identical).
   - **Section C (identity): OBSERVED LIMITATIONS** — C.7 name change → new Record ID → new row, old orphaned. C.8 repeated movement (same Name+Direction) → same Record ID → 2nd movement **silently overwrote** the 1st (data lost). `(Name,Direction)` insufficient for multi-movement travelers.
   - **Section D (schema/data mutation, on copy):** D.1 human column right of N = SAFE; **D.2 delete/reorder managed A:N column = UNSAFE & SILENT** (force-rewrites header, misaligns data); D.3 manual row = safely handled (adopted if identity matches); **D.4 delete managed row = UNSAFE but RECOVERABLE** (re-run TMO recreates it).

---

## Final state

**① Dispatch — Phase C built, live-verified, PR #2 open (unmerged).** Core flow works end-to-end on real Google Sheets. Identity (C.7/C.8) and schema-mutation (D.2/D.4) limitations recorded for the architecture decision. Branch/PR held unchanged at owner's request.

---

## Open Items / Next Time

- **2026-09-11: Dispatch final review / architecture decision** using the C.7/C.8 (identity) + D.2/D.4 (schema/deletion) findings. Menu to decide: canonical Trip/Movement ID (fixes C.7/C.8), schema validation on every run + protected ranges on A:N/Record ID (fixes D.2), duplicate Record ID check, recovery snapshots/backup cadence (D.4). Then review/merge PR #2.
- **Confirm** whether the 2026-09-10 09:00 Test Traveler A/B notification actually fired on-device.
- **Cleanup (pending, do NOT do yet):** primary sheet 7 test rows + `Ops Note` column; `Verify Delta` Calendar event (2026-11-18); mock TMOs `Reference Files/MOCK_Sheets_*`, `MOCK_C7_*`, `MOCK_C8_*`, `MOCK_D3_*`; old Test Traveler A/B rows/events. Test Traveler A/B calendar test intentionally left untouched.
- **Then:** next agent = ⑤ Hotel Ops (`05. Hotel Ops/`).

---

## Files (in `02. Dispatch/`)

- New: `dispatch_agent/sheet_store.py`, `tests/test_sheet_store.py`, `SETUP_GoogleSheets.md`, `DESIGN_Dispatch_PhaseC_Sheets.md`
- Changed: `dispatch_agent/cli.py`, `dispatch_agent/config.py`, `tests/test_cli.py`, `tests/test_config.py`, `SETUP_GoogleCalendar.md`
- GitHub: `audgk52/appa-travel-automation` PR #2 (base `main` ← `phase-c-sheets`, commit `e9cbd4a`), not merged. 96 tests passing.
- Env: `APPA_GSHEET_ID` added to `~/.zprofile` (primary sheet); Sheets API enabled in GCP project `appa-dispatch`.
