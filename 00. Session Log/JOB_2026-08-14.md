# JOB — 2026-08-14 (APPA Agent Automation)

**Session:** Dispatch Phase B (Google Calendar reminders) — design → build → review → merge → live setup, 2026-08-14 KST
**Working folder:** `~/Desktop/2026 취준/Travel Automation/`

> Note: requested as "JOB 8/16" but dated to the actual work day (8/14) to match the 08-11/12/13 sequence. Rename if you prefer 8/16.

---

## Key Points (what happened)

1. **Brainstormed + designed Phase B** as a **Calendar-only** increment (Google Sheets migration deferred). Decisions: **service account** auth; **timed 09:00–09:15** event on each Send Date; **dedicated "APPA Dispatch" calendar**; KakaoTalk message in the event body. Wrote `DESIGN_Dispatch_PhaseB_Calendar.md` + `PLAN_Dispatch_PhaseB_Calendar.md`.
2. **Executed via subagent-driven TDD** (fresh implementer per task + spec-compliance and code-quality reviews). Built:
   - `calendar_sync.py` — deterministic **base32hex** event IDs (`event_id_for`), pure `build_event`, injectable `CalendarSync.upsert_event` (GET→insert/update, 409 race), lazy-import `build_calendar_service` (offline-safe).
   - `config.calendar_config()` — `APPA_*` env vars.
   - CLI wiring — two-tier degradation (disabled vs configured-failure).
3. **Senior implementation review R1 = merge gate.** Applied all must-fix, test-first:
   - **Reminder ownership (BLOCKER):** events now use `reminders.useDefault=true` — a service-account popup override does not notify the human; the coordinator's alert comes from the **calendar's own default notification**.
   - No `id` in the `events().update()` body; partial/invalid config is a **failure not offline mode**; invalid `APPA_GCAL_HOUR` can no longer bypass local save; whitespace trimmed in `event_id_for`; secret `.gitignore` patterns; full CLI configured-failure test (exit≠0 + WARN + local row persisted); `requirements.txt`; `SETUP_GoogleCalendar.md`. (Owner completed review R2.)
4. **Merged PR #1 → `main`.** Full suite **73 passing**.
5. **Google setup completed and LIVE-VERIFIED:** GCP project + Calendar API, service account key at `~/.appa/appa_service_account.json` (600), "APPA Dispatch" calendar shared with the SA as "Make changes to events", calendar default notification set, env vars persisted in `~/.zprofile`. A live test event's notification **fired at 01:39 KST on my own device** — the R1 reminder-ownership blocker confirmed working in practice.
6. **Mock memo for a real 09:00 test:** created `Reference Files/MOCK_Travel_Memo_TEST_081626.docx` (dispatch 8/16 → Send Date 8/14). The real CLI created pickup + send-off events; the **pickup reminder fires 8/14 09:00 KST** (pending human confirmation).
7. **Rebuilt `master_schedule.xlsx`** from all reference memos (local-only, calendar off): 10 memos → 20 upserts → **14 rows** (revisions collapsed to latest **BLUE/PINK**), sorted by Send Date. Deleted the stale **pre-redesign two-tab** sheet.

---

## Final state

**① Dispatch — Phase B (Calendar) shipped on `main`.** Calendar sync is live and verified end-to-end. `master_schedule.xlsx` rebuilt from the current corpus. 73 tests passing.

---

## Open Items / Next Time

- **Confirm** the 8/14 09:00 pickup notification, then clean up the mock (`MOCK_Travel_Memo_TEST_081626.docx` + its two Test Traveler calendar events + sheet rows).
- **Next increment — Google Sheets migration:** replace the local `ScheduleStore` with a `GoogleSheetStore` (same `upsert(row) -> inserted|updated` contract) so the master schedule is cross-device/shareable. Then optional team calendar subscription + bulk backfill.
- **Then:** continue the method-first agent order (⑤ Hotel Ops next) toward the multi-agent skeleton.
- **Hardening phase (per `TEST_STRATEGY_Dispatch_EN.md`, deferred until skeleton stable):** full historical-corpus validator (structured report, silent-failure detection), stratified UAT matrix, cross-agent integration tests. Do **not** build the corpus validator yet.

---

## Files (in `02. Dispatch/`)

- `dispatch_agent/`: `calendar_sync.py` (new), `config.py`, `cli.py`, `records.py`
- `tests/`: `test_calendar_sync.py` (new), `test_config.py`, `test_cli.py`
- `DESIGN_Dispatch_PhaseB_Calendar.md`, `PLAN_Dispatch_PhaseB_Calendar.md`, `SETUP_GoogleCalendar.md`, `requirements.txt`
- Review records: `REVIEW_CalendarSync_PhaseB*.md`, `REVIEW_CalendarSync_PhaseB_Implementation_R1_*.md`, R2, `TEST_STRATEGY_Dispatch_*.md`
- `master_schedule.xlsx` (rebuilt; gitignored) · GitHub: `audgk52/appa-travel-automation` (PR #1 merged)
