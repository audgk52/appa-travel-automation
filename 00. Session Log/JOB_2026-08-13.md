# JOB — 2026-08-13 (APPA Agent Automation)

**Session:** Dispatch Phase-A hardening on real memos (continuation of 8/12), 2026-08-13 KST
**Working folder:** `~/Desktop/2026 취준/Travel Automation/`

---

## Key Points (what happened)

1. **Resumed from the 8/12 chosen next step:** harden Dispatch Phase A on more real memos, test-first, before building Phase B. Green baseline confirmed first (21 tests).
2. **Ran the real memos through the parser and found 5 issues** (2 more than the log predicted). Three were *silent* failures that produced **no dispatch at all**:
   - **GMP not treated as a Korean airport** — `korea_departure`/`korea_arrival` only matched `ICN`, so Gimpo trips (Gayoung Kim, Jon Ko) generated nothing.
   - **3-letter airline prefixes dropped** — the flight regex expected a 2-char code, so every `ANA ###` leg (Jon Ko) was silently skipped (0 legs parsed).
   - **Alternate city format** — `Incheon Int, … ICN Terminal 1` (no slash) parsed the *city name* instead of the IATA code (Carey Mumford) → ICN undetected.
   - **GMP/None-terminal address** would `KeyError` once GMP was recognised.
   - **Multi-passenger/family** cell captured as one messy blob; role/cast lost.
3. **Locked 3 decisions with the user** (AskUserQuestion):
   - **GMP send-off lead = departure − 3h** (ICN stays −4h). Validated against Jon Ko's own memo (04:40 = exact match).
   - **특이사항 auto-fills** the memo's role/cast label; the ad-hoc note is **parenthesised** after it — matching the real Silia Kapsis message `Cast #14 & 엄마 (짐 개수 스타리아 1대 문제 없음)`.
   - **탑승자 = all traveler names, comma-joined** (per Silia example); cast/guest labels go to 특이사항, not names.
4. **Fixed test-first (TDD):** wrote RED tests → implemented → GREEN. `memo.py` (GMP set, 3-char flight regex, IATA extraction, `_parse_passengers` for family + role-only lines), `config.py` (airport-level fallback address + `sendoff_lead_hours`), `records.py` (`compose_notes` + `lead_hours` param), `builder.py` (per-airport lead, joined travelers, composed notes).
5. **Verified end-to-end:** ran the CLI on all 4 memos; pick-up = landing and send-off = −lead all reconcile with the memos' own car-service tables.
6. **Two more edge cases raised + locked with regression tests:**
   - **Alphanumeric airline prefix** (e.g. `B6 1044` JetBlue, `5J`) — already parsed correctly by the `[A-Z0-9]{2,3}` flight regex; header/disclaimer still rejected.
   - **One-way memo** (only a departure *or* only a return leg) — `directions_in_memo` returns just the present direction, the missing side is `None`, and only one dispatch message is generated.

---

## Final state

**Tests: 44 passing** (was 21). New suites: `test_memo_hardening.py`, `test_config.py`; extended `test_records.py`, `test_builder.py`, `test_cli.py`.

Phase A now handles: ICN + GMP, 2-/3-letter and alphanumeric airline codes, both city formats, missing/TBD terminals (→ no specific terminal), multi-passenger/family memos, and one-way (single-direction) memos.

---

## Open Items / Next Time

- **Not yet committed** — review, then commit + push to the private repo.
- **Cosmetic:** Carey's group note renders `Cast #2 & Cast #2 Guests` (faithful to the two label lines, editable). Dedupe to `Cast #2 & Guests` only if it bothers you.
- **Confirm GMP address granularity** — currently one generic `김포공항 국제선` line (no specific terminal, per the size rule). Split domestic/intl only if a domestic GMP leg ever appears.
- **Phase B (next build):** migrate the master sheet to **Google Sheets** + create **Google Calendar** events on each Send Date. Needs one-time Google OAuth.
- **Deferred (from 8/12):** airline→terminal fallback map · 동승 (shared-car) grouping · PE-4 full diff summary + re-dispatch flag · baseline time-saved metrics.

---

## Files touched (in `02. Dispatch/`)

- `dispatch_agent/`: `memo.py`, `config.py`, `records.py`, `builder.py`
- `tests/`: `test_memo_hardening.py` (new), `test_config.py` (new), `test_records.py`, `test_builder.py`, `test_cli.py`
