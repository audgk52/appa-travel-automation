---
description: Start of a fresh session — load today's state and continue where the last session stopped
---

Read, in order: `STATUS.md`, `NEXT.md`, and the most recent entries of `WORKLOG.md` (if it exists).
Then run `git status` and `git log --oneline -5`.

Reply briefly (max ~10 lines):
1. Where we are: current NEXT.md task and its state.
2. Anything in WORKLOG.md that conflicts with git (e.g. "committed" but no such commit, uncommitted changes).
3. The exact next step you propose.

Then wait for Myungha's go-ahead before changing anything. $ARGUMENTS
